# Vplugins Request

Compatibility entry node for API-format workflows.

It loads a main image, optional mask, prompt, and JSON parameters from either
ComfyUI's input directory or the active PS Bridge request.

For new bridge templates, prefer `Adv_Request`.
