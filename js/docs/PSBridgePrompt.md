# PS Bridge Prompt

Reads a named prompt slot from the latest bridge request.

- `slot_id`: parameter name, such as `positive`
- `fallback`: value used when the request does not contain that slot

The output changes when a new bridge request arrives.
