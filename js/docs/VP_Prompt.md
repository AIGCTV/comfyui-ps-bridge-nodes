# PS Prompt

经典画布点击文本区域可原位编辑，支持中文输入法、选中文字与复制粘贴。Ctrl+Enter 提交并结束编辑，普通 Enter 换行。结束后由 Canvas 显示文字，长文本可在区域内滚动；中文组合输入未完成时不会开始节点拖动或排队。

唯一多行输入框、唯一 STRING 输出。中文、空格、换行、空文本和特殊字符原样保存，不做动态提示词展开。组合输入结束前不导出半成品。

类别：🔷PS Vplugins。旧节点/端口布局不兼容，不自动迁移。
