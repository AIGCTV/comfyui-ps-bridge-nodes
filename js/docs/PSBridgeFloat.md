# PS Bridge Float

Reads a named floating-point parameter from the latest bridge request.

Use `fallback` when the slot is absent. `min_value`, `max_value`, and `step`
describe the intended control range for compatible clients.
