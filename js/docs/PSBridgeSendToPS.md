# PS Bridge Send To Photoshop

Legacy output-node alias retained so existing workflows continue to load.

It returns an image and optional alpha mask to the active Photoshop request.
For new workflows, use `Adv_SendToPS`.
