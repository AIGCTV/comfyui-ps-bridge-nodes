# PS Bridge Image Input

Loads one image slot from the latest Photoshop bridge request.

Outputs the RGB image, selection mask, opacity alpha, width, and height. Use
`Image 1` for compatibility with older single-image requests.

For new multi-image templates, prefer `Adv_Request`.
